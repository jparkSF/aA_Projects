import React from "react";
import TodoListContainer from './todos/todo_list_container';

const header = () => {
  return(
    <div>
        <TodoListContainer/>
    </div>
  );
};

export default header;
